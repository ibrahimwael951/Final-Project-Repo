// var scrollGap = 1
// var prevScrollPos = 0
// var currentFrame = 1
// var frameCount = 500

// window.addEventListener('scroll', function() {
//     var image = document.getElementById('scrollImage');
//     // if (this.window.scrollY > prevScrollPos + scrollGap){
//         currentFrame = Math.ceil(window.scrollY / this.window.innerHeight * frameCount);
//         if (currentFrame == 0) currentFrame = 1
//         image.src = ' ../../assets/rendering/'+ currentFrame.toString().padStart(4, '0') + '.png'

//         prevScrollPos = window.scrollY; 
//     // }

// });